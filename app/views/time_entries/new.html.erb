<h2>Add a Time Entry</h2>
<%= form_for [@my_project, @my_entry] do |f| %>
		<%= f.label :hours %>
		<%= f.number_field :hours %>

		<%= f.label :minutes %>
		<%= f.number_field :minutes %>

		<%= f.label :date %>
		<%= f.date_field :date %>

		<%= f.submit "Save" %>
<% end %>